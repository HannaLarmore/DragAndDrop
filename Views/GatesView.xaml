<UserControl x:Class="DragAndDrop.Views.GatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DragAndDrop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Column="0" Grid.Row ="0" FontSize="20" Text="Logic Gates" HorizontalAlignment="Center"/>
        <Rectangle Grid.Column="0" Grid.Row ="1"  Fill="Black" Height="2" VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="0,5,0,5"/>
        <ContentControl Grid.Column="0" Grid.Row ="2" Content="{Binding TempANDGate}"/>

    </Grid>
</UserControl>
